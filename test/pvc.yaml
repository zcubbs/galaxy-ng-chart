---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: galaxy-ng-pvc
  namespace: galaxy-ng
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
